import React from 'react';

const ErrorMessage = (() => {
  return (
    <div className="main-content align-center">Sorry! There was an error loading the item.</div>
  );
});

export default ErrorMessage;
